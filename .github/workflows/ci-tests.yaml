on: [push, pull_request]

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    name: Static Analysis (PHP 8.3)

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: PHPStan
        run: ./vendor/bin/phpstan analyse

      - name: PHP-CS-Fixer
        run: ./vendor/bin/php-cs-fixer fix --dry-run --diff

  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']
        scenario: ['behat3', 'behat4']
        include:
          - php: '8.6'
            scenario: 'behat3'
            experimental: true
          - php: '8.6'
            scenario: 'behat4'
            experimental: true

    name: PHP ${{ matrix.php }} / ${{ matrix.scenario }}
    continue-on-error: ${{ matrix.experimental == true }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Install dependencies
        run: |
          composer install --no-interaction --prefer-dist
          composer scenario ${{ matrix.scenario }}

      - name: Run PHPUnit
        run: composer tests:unit

      - name: Start PHP built-in server
        run: php -S localhost:8531 -t app &

      - name: Run Behat
        run: |
          OUTPUT=$(./vendor/bin/behat --no-colors 2>&1)
          echo "$OUTPUT"
          # fail.feature contains 1 intentionally failing scenario run across 3 suites = 3 expected failures
          echo "$OUTPUT" | grep -q "9 scenarios (6 passed, 3 failed)" \
            || { echo "Unexpected behat scenario result!"; exit 1; }
